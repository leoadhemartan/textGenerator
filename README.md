# textGenerator
Generates a string with a certain character count with a defined set of characters

I needed to create a string of random words with a certain character count that's limited to a particular character set while testing an AFTN messaging system.
